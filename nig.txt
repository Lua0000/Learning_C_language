1. Indique quais das seguintes declarações estão corretas.
a) y int; Incorreta
b) int; Incorreta
c) integerx; Incorreta 
d) inta, b; Incorreto 
e) float f, g , c; Correto 
f) char chl=ch2-A'; Incorreto 
g) char chi = 'A', ch2 = ’A’; Correto 



2. Uma variável inteira, quando é declarada, é sempre iniciada com ...
a) 0 (Zero)
b) 1 (Um)
c) Um valor aleatório
d) Um valor negativo

resposta = C

3. Indique, na seguinte lista, quais os identificadores corretos e incorretos de variáveis:
a) Valor = Correto 
b) &XN2X = Incorreto 
c) dez% = Incorreto
d) a+b = Incorreto
e) _Kabonga = Correto
j) MENOS = Correto 
g) 10a = Incorreto 
h) alO = Correto
i) main = Incorreto
j) F1 = Correto 

4. O nome de uma variável...
a) deve indicar aquilo que ela armazena - Correto 
b) deve ser o menor possível - Incorreto
c) deve ser o maior possível - Incorreto
d) deve ser o mais explícito possível - Correto 
e) deve ser todo escrito em maiúsculas - Incorreto 
f) pode, mas não deve, começar por underscore Q - Correto
g) pode conter mais do que um caractere - Correto 

5. Associe os seguintes tipos aos correspondentes formatos de leitura e escrita.
int %e
float %ld
char %f
short int %d
long int %hd
signed long int %c
R = int = %d, float = %f, char = %c, long int = %ld, short int = %hd,  singed long int = %ld 

6. Indique quais das seguintes afirmações são verdadeiras e quais são falsas.
a) O tipo float reserva espaço em memória para um real com precisão simples, enquanto o tipo
double reserva espaço para uma variável com precisão dupla.
b) O tipo char pode ter os prefixos long e short.
c) O tipo char pode ter os prefixos signed e unsigned.
d) Uma variável declarada como unsigned pode comportar valores superiores a uma outra que seja
declarada como signed.
e) Uma variável do tipo char pode armazenar caracteres individuais ou conjuntos de caracteres
também denominados por strings.
f) Uma variável do tipo char pode armazenar vários caracteres, desde que todos eles sejam caracteres ASCII.
g) Uma variável do tipo char pode armazenar vários caracteres, desde que sejam caracteres especiais.
h) O operador módulo (%) não pode ser utilizado em reais.

Resposta = A = V  , B = F, C = V, D = V, E = F, F = F, G = F, H = V

7. Identifique os erros de compilação que seriam detectados nos seguintes programas:

7.1

#include <stdio.h>
main () {
    int x, y, x;

}

r = Variavel X declarada duas vezes

7.2
#include <stdio.h>
main ()
{
    int x, y;
    float int = 5.23;
    printf ("%f",int);
}

R = Int é uma palavra reservada da linguagem C portanto não pode ser usada para criar variaveis,
Declarou variavel inteira mas atribuiu a um numero float.

7.3

#include <stdio.h>
main ()
{
int x=y=z=0;
printf(”%d %d %d \n",x,y,z);
2
}

R = O código não tem relação com o escopo, e não é desse jeito que atribui um valor a varias variaveis,
o correto é int x, y, z;
 x = y = z = 0;

 8. Embora os programas que se seguem não tenham erros de compilação, identifique as causas de seu
possível mau funcionamento.

8.1 

#include <stdio.h>
main ()
{
int n;
scanf("Introduza um N° %d”, &n);
printf("O n° = %d\n", n);
}

R = Ele está usando o Scanf como se fosse um printf, sendo que o scanf serve para armazenar um valor.

8.2 

main ()
{
int n;
printf("Introduza um Non);
scanf("%d\n", &n);
printf("0 n° = %d\n",n);
}
R = O código está certo em algumas partes, porém usar \n dentro do scanf faz o scanf esperar
mais digitos, eis a forma correta:

#include <stdio.h>

int main() {

	int n;
	printf("Digite um valor: ");
	scanf(" %d", &n);
	printf("O numero: %d\n", n);
	return 0;
}

8.3 

#include <stdio.h>
main()
{
int n;
printf("Introduza um Non);
scanf("%f", &n);
printf("0 n° = %f\n",n);
}

R = Falta o int main(), também falta aspas duplas no final do printf, o scanf
está lendo um valor float sendo que a variavel é inteira.

9. Escreva um programa que solicite ao usuário uma determinada data e amostre em seguida no formato
dd/mm/aaaa.

R = #include <stdio.h>

int main() {
	
	int Dia, Mes, Ano;
	
	printf("Digite uma data: ");
	scanf("%d", &Dia);
	
	printf("Digite um mes: ");
	scanf("%d", &Mes);
	
	printf("Digite um ano: ");
	scanf("%d", &Ano);
	
	printf("A data: %d/%d/%d\n", Dia, Mes, Ano);
	return 0;
}

10. Escreva um programa que solicite ao usuário uma determinada data no formato aaaa-mm-dd e a
mostre em seguida no formato dd/mm/aaaa.

R = #include <stdio.h>

int main() {
	
	int Ano, Mes, Dia;
	
	printf("Ano: ");
	scanf("%d", &Ano);
	printf("Mes: ");
	scanf("%d", &Mes);
	printf("Dia: "); 
	scanf("%d", &Dia);
	
	printf("Formato AAAA-MM-DD: %d-%d-%d Formato DD-MM-AA: %d/%d/%d", Ano, Mes, Dia, Dia, Mes, Ano);
	return 0;
}